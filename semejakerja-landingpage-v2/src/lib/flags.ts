// Feature flags. Flip to true when the feature is ready to ship.

// Student membership: register checkbox, dashboard verification badge, and
// student promo copy on checkout. Hidden until KTM verification is built.
export const STUDENT_MEMBERSHIP_ENABLED = false;
